{
  "name": "virtual-study-group",
  "version": "1.0.0",
  "description": "AI-powered virtual study group platform",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && set HOST=0.0.0.0&& set HTTPS=true&& set SSL_CRT_FILE=../backend/certs/cert.pem&& set SSL_KEY_FILE=../backend/certs/key.pem&& npm start",
    "build": "cd frontend && npm run build",
    "install-deps": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "start": "cd backend && npm start"
  },
  "keywords": ["study", "group", "virtual", "ai", "education"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
